# 2026-09-11 푸시·운영 배포 사전 검토

판정: 로컬 코드·운영용 빌드 검증 통과. **보상 DB 마이그레이션은 사용자 승인으로 2026-09-11 18:11:42(한국 시간) 운영 적용 완료**했다. Git 푸시·앱 배포는 아직 실행하지 않았다. 현재 사용자 세션에 대한 무중단을 절대적으로 보장하는 검증은 아니며, 아래 순서와 적용 후 확인이 필요하다.

## 확인한 운영 기준

- 원격 `main`과 로컬 HEAD: `3456209aed14dc00b360502b2c0b144033d94253` 일치.
- 운영 도메인: `https://app.ronparkpass.com`.
- 현재 정상 배포: `dpl_2DRismDBgEpFLnBb2U8EdtP5ZqDQ` / `https://outing-auth-jwbizf40a-ronpark.vercel.app`.
- Supabase: RONPARK APP / `ehzrwkvuarertwvwoyus`, ACTIVE_HEALTHY.
- 운영 홈·교사 진입·서비스워커·설정 API GET 200. 스터디카페 학생·관리자 API의 GET 405는 POST 전용 API의 정상 응답.
- 운영 DB 확인 시 학습 중 세션 16개, 기존 봇 5개. 최근 1시간 봇 cron 60회 성공, 실패 0회. 사용자 레코드·학습 상태를 바꾸는 운영 테스트는 수행하지 않았다.

## 배포 전 필요한 DB 변경

| 마이그레이션 | 운영 상태 | 이번 작업 |
| --- | --- | --- |
| `20260911052853_add_study_character_hair.sql` | 적용됨 | 재적용하지 않음 |
| `20260911053530_add_study_cafe_feedback_hub.sql` | 적용됨 | 재적용하지 않음 |
| `20260911072426_sell_character_hairstyles.sql` | 적용됨 | 재적용하지 않음 |
| `20260911091142_add_student_welcome_rewards.sql` | **적용 완료** | 사용자 승인 후 적용·검증 완료, 재적용하지 않음 |
| `20260911080239_add_study_cafe_bot_admin.sql` | 적용됨 | 재적용하지 않음 |

최초 사전 검토에서는 보상 테이블·RPC가 없었으며, 이후 사용자 요청으로 적용했다. 운영 이력의 실제 버전 `20260911091142`에 맞춰 로컬 파일도 기존 `20260911072658`에서 이름을 정리했다. SQL 본문은 검증한 내용 그대로다. 신규 테이블 4개·함수 4개·트리거 2개와 권한을 확인했다. 기존 등록자 279명이 제외되었고 누락은 0건이다. 적용 직후 학생 319명·지갑 93개·포인트 내역 1,184건은 적용 전과 같으며 신규 보상 지급 0건이다. service_role의 기존 수강생 보상 조회를 롤백 검증했고 `ok: true, eligible: false`로 응답했다. 적용 후 봇 cron 정상 실행과 보안 검사 ERROR/WARN 없음도 확인했다. 보상은 신규 가입자 웰컴 100P, 3일 연속 하루 5시간 달성 300P이다.

SQL의 트랜잭션, 3초 잠금 대기 제한, 30초 문장 제한을 유지한다. 학생·학습 세션 테이블에 트리거를 설치하므로 짧은 잠금 대기가 생길 수 있다. 혼잡하거나 잠금 획득에 실패하면 중단하고 한산한 시점에 다시 확인한다. 전체 스키마, 봇 최초 설치 SQL, 이미 적용된 마이그레이션을 일괄 재실행하지 않는다.

## 기존 사용자 영향 및 이번 보완

- 사용자가 이번 검토에서 ‘스터디방 목록’, ‘랭킹룸·자유석 전환’ 두 버튼을 숨기는 동작을 명시적으로 선택했다. 설정이 없는 운영 환경에서 기본 숨김을 유지한다. 좌석·기록 데이터 삭제는 없다.
- 기존 로그인, 기기 인증, 출석·외출·성적 저장 API는 교체하지 않는다. 신규 보상 동기화는 별도 요청으로 처리되며 실패가 로그인·타이머 성공을 실패로 바꾸지 않는 구조를 확인했다.
- 현재 앱에는 서비스워커 변경 시 열린 화면을 강제로 새로고침하는 처리가 없다. 구형 클라이언트의 프로필 변경이 새 머리 데이터를 지우지 않는 테스트와 기존 타이머 복구 테스트가 통과했다.
- 서비스워커의 과거 파일 중복을 정리하고 두 HTML에서 사용하는 최신 정적 파일을 모두 등록했다. 최신 관리자 JS 누락을 보완하고 캐시 버전을 `v425-release-review`로 갱신했다.
- 신규 보상·상점·봇 관리자 JS의 재검증 캐시 헤더를 추가했다.
- Vercel 배포에서 필수 공개 환경변수가 없거나 URL이 잘못되면 기존 설정 파일을 덮어쓰기 전에 빌드를 실패시키도록 보완했다. 기존 로컬 미리보기의 빈 설정 동작은 유지한다.
- 임시 빌드·브라우저 프로필·Supabase CLI 임시 상태를 Git 또는 배포 대상에서 제외했다. 소스 후보 파일의 서비스 역할 JWT·비밀키 패턴 검사에서 검출은 없었다. 이 검사는 모든 형태의 비밀정보를 보장해서 찾는 검사는 아니다.

## 검증 결과

- `npm.cmd test`: 63개 테스트 파일 실행 성공. 출석·외출·성적·학생 유형·교사 권한·기기·푸시·타이머 복구·새 기능·보상·캐시·빌드 설정 검사 포함.
- 기본 명령 밖의 로컬 질문 게시판, 교사 체력 성적 지연 로딩 검사 2개 성공.
- 격리 PGlite DB 검사 2개 성공: 보상 자격·중복 지급·원자적 적립·자정·중단·연속 날짜·권한, 봇 과거 정산·구매 원자성·운영 설정·자동 실행.
- 루트·API JavaScript 60개 `node --check` 성공.
- 격리된 `.tmp/release-build`에서 `vercel build --prod` 성공. 함수 번들 31개, 정적 산출물 72개. 주요 변경 API 4개의 번들 의존성 로드 성공. 비공개 환경파일·로컬 상태·테스트·문서·DB SQL은 정적 산출물에서 제외됨을 확인했다.
- 공개 운영 `/config.js`의 프로젝트와 anon 키 역할을 확인하고 그 공개 값만 넣어 최종 로컬 빌드를 검증했다. 이 폴더는 **검증용**이며 여기서 배포하지 않는다.
- 기존 브라우저 QA 스크립트: 실제 캐릭터 렌더러 320·390·768px, 경계·불꽃 단계·타이머 갱신·정지·학생 유형 범위·장비·동작 줄이기 통과. 실제 localhost 스터디카페의 안내 버튼·6단계 안내·모바일 경계·열기/닫기도 통과했고 생성 이미지 확인.
- Supabase 보안 검사 ERROR/WARN 없음. 서비스 역할 전용 테이블의 RLS 정책 없음 INFO는 일반 클라이언트 접근 차단 구조와 일치한다. 신규 의견 테이블의 anon/authenticated/PUBLIC 직접 권한 없음 확인.
- 성능 검사에는 기존 `students`, `outings` UPDATE RLS 정책 중복 WARN 2건이 남는다. 이번 변경이 해당 정책을 바꾸지는 않는다. 검토 중 권한 정책을 변경하지 않았다. [Supabase 설명](https://supabase.com/docs/guides/database/database-linter?lint=0006_multiple_permissive_policies).

## 검증 한계

모든 실제 사용자 기기, iOS 설치형 앱, 전체 관리자 모달의 수동 검수는 완료한 것이 아니다. 연결된 브라우저 조작 도구는 이용할 브라우저를 찾지 못했다. 화면 검증 범위는 위의 자동화와 캡처 확인에 한정된다. 운영 계정으로 로그인·출석 제출·시험 제출·타이머 시작/종료·구매를 수행하지 않았다.

로컬 운영 환경파일은 가려진 값이어서 그대로 사용할 수 없었다. 자동 승인 검토가 비공개 운영 환경변수 전체 다운로드를 거절했으므로 수행하지 않았다. 대신 Vercel의 운영 환경변수 이름·설정 여부와 앱에 공개된 설정을 확인했다. 따라서 인증·푸시 비밀값의 실제 내용까지 재검증한 것은 아니다. 기존 설정을 유지해 실제 클라우드에서 빌드해야 한다.

## 최종 승인 후 적용·복구 순서

1. 원격 main과 현재 운영 배포를 다시 확인하고 현재 배포 ID를 복구 대상으로 보관한다.
2. 보상 마이그레이션은 적용·검증 완료했다. 이후에는 적용 상태만 재확인하고 다시 실행하지 않는다.
3. 검토된 전체 변경사항을 커밋·푸시한다. Git 연동으로 운영 배포가 시작되면 그 배포를 추적하고 별도의 중복 배포를 만들지 않는다. 자동 배포가 시작되지 않으면 기존 프로젝트의 운영 환경설정을 사용하는 배포를 한 번 수행한다. 검증용 `.tmp` 폴더나 가려진 로컬 환경파일을 배포에 사용하지 않는다.
4. 새 배포가 Ready인지, 운영 도메인·정적 파일·캐시 버전·API 응답과 오류 상황을 확인한다. 기존 사용자의 세션을 강제로 새로고침하거나 로그아웃시키지 않는다.
5. 새 버전 문제가 확인되면 위 정상 배포로 앱을 복구한다. 이미 존재하는 학생·학습·지갑·구매·보상 기록을 삭제하거나 DB 전체를 되돌리지 않는다. 보상 트리거에서 문제가 발생한 경우 해당 신규 트리거만 비활성화하는 별도 복구를 검토한다.

보상 DB 변경은 사용자의 별도 승인으로 완료했다. 프로젝트 `AGENTS.md`의 ‘푸시와 배포는 비용이 발생하므로 사용자의 최종 확인을 받은 후 진행한다’ 지침에 따라 Git 푸시·앱 배포는 최종 확인 대기 상태다.
